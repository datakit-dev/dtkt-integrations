apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: bigquery
profiles:
- name: bigquery
  build:
    local:
      useBuildkit: true
    artifacts:
    - image: bigquery
      context: ../../../
      docker:
        dockerfile: dtkt-integrations/packages/bigquery/Dockerfile
  manifests:
    kustomize:
      buildArgs:
      - --enable-alpha-plugins
      - --enable-exec
      paths:
      - infra/kustomize/overlays/dev
  deploy:
    kubeContext: kind-dtkt-local
    kubectl:
      defaultNamespace: dtkt-integrations
  portForward:
  - resourceType: service
    resourceName: bigquery
    port: 80
    localPort: 8200
