apiVersion: skaffold/v4beta12
kind: Config
metadata:
  name: mailpit
build:
  local:
    useBuildkit: true
  artifacts:
  - image: mailpit
    context: ../../../
    docker:
      dockerfile: dtkt-integrations/packages/mailpit/Dockerfile
manifests:
  kustomize:
    buildArgs:
    - --enable-alpha-plugins
    - --enable-exec
    paths:
    - infra/kustomize/overlays/dev
deploy:
  kubeContext: kind-dtkt-local
  kubectl:
    defaultNamespace: dtkt-integrations
# portForward:
# - resourceType: service
#   resourceName: mailpit
#   port: 80
#   localPort: 8200
