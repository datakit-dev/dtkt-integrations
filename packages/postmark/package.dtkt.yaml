# yaml-language-server: $schema=http://localhost:9900/schemas/dtkt.integrationsdk.v1beta1.Package.jsonschema.json
kind: Package
apiVersion: v1beta1
spec:
  identity:
    name: Postmark
    version: 0.0.0
  description: Send individual, batched, streamed, or templated emails through Postmark's API.
  icon: https://postmarkapp.com/images/logo-stamp-social.png
  type: PACKAGE_TYPE_GO
  build: {}
